# 이연순 명인떡 홈페이지 — 인수인계 (2026-08-17)

> 새 창에서 시작할 때 이 문서만 읽으면 이어집니다.
> 진행 기준은 `관리고객사\소통마케팅센터\교재\바이브코딩_홈페이지_강의교재V4.docx` 8교시 커리큘럼.
> 작업 중 발견한 교재 누락·함정은 같은 폴더 `교재V4_보완사항.md` 에 누적 (현재 18건).

---

## 고정값 (절대 헷갈리지 말 것)

| 항목 | 값 |
|---|---|
| 저장소 | `choikiho20-cyber/myeongin-tteok` |
| 브랜치 | **`master`** (main 아님) |
| Pages 소스 | **저장소 루트 `/`** (docs/ 아님 — 자동발행이 깨짐) |
| 주소 | https://myeongin-tteok.com |
| 로컬 | `C:\Users\user\Desktop\sotong-sites\myeongin-tteok` |

### 확정된 매장 정보 (2026-08-17 확인)
- 상호: 이연순 명인떡 / 대표: 이연순 (대한민국 식품명인 제52호, 2013 지정)
- 주소: 충북 제천시 제천북로 138 **1층** (왕암동)
- **전화: 043-645-3050** — 매장 유선, 통화 가능. 착신전환 불필요
- 영업시간: **화~일 09:00–19:00 / 월요일 휴무** ← **홈페이지가 정확**
  (스마트스토어·구글에 다르게 적혀 있으나 고객사가 홈페이지 기준이라고 확인. 나머지는 차차 수정)
- **3대째** (MBC 영상 제목의 "4대째"는 방송사 표기 오류)
- 배송비 4,000원

### 도메인·토큰
- 가비아, 2026-08-10 등록 ~ **2027-08-10 만료**, 연장 알림 설정 완료(SMS)
- 소유자 **이연순** `leeys3050@naver.com` / 관리자 **최기호** `choikiho20@gmail.com`
- 소유자 이메일 인증 — **완료됨. 추가 조치 불필요**
- ⚠️ 가비아 `소유자 추가 인증` 버튼은 **누르지 말 것** — 인증이 아니라 소유자 정보를 *변경*하는 기능
- ⚠️ 가비아 메일 하단 `1661-4370` 은 하이웍스 라인이라 도메인 상담 불가
- GitHub 토큰: fine-grained, 만료 2027-08-10. **평문 노출 이력 있어 재발급 권장**

---

## 지금 바로 할 일

### 1. 상품 가격 회신 대기 ← 여기부터
`관리고객사\카페 떡\서류\상품가격_확인요청.txt` 를 실장님께 발송했거나 발송 예정.
회신 오면 아래를 반영한다.

| 미확정 | 현재 상태 |
|---|---|
| 깨소금 인절미 택배가 | 매장 3,500원만 표시 |
| 두텁떡·오란다·호두정과·동글이강정 | 4종 모두 "가격 문의" |
| 승검초단자 30구 (60,000원) | 홈페이지엔 있는데 **스토어 미등록** → 뺄지 결정 필요 |
| 쑥 찹쌀떡의 소 | 팥? 녹두? 포장엔 "제천 약초떡" |

### 2. 글해줌 매장 정보 입력 → 시험 발행 1건
글해줌(https://geulhaejum.kr) 명인떡 매장 페이지의 **매장 설정 9개 칸(store_fields)이 미입력.**
정보 없이 발행하면 AI 가 빈 내용으로 글을 쓰고 그대로 저장소에 박힌다.

붙여넣을 내용은 `관리고객사\카페 떡\서류\글해줌_매장정보_입력안.txt` 에 준비됨
(2026-08-18 작성, 확정정보만. 미확인 항목은 일부러 비웠다).
저장 경로: 화면 저장 → `clients/이연순명인떡.json` 의 `store_fields`.
DB 가 아니라 이 json 이 정본이다 (DB 에는 industry 만 들어간다).

발행 후 아래 5가지를 실측할 것:
1. `posts.json` 에 id 2 추가
2. `posts/2.html` 생성 + 템플릿 7개 항목 유지
3. **새 글 주소가 myeongin-tteok.com 인지**
4. `sitemap.xml`·`robots.txt`·`llms.txt`·`feed.xml` 갱신
5. 브라우저 렌더링

### 3. 검색엔진 마무리
- 구글 서치콘솔 개별 색인 요청 (신규 페이지 gallery/media 포함)
- 네이버 웹페이지 수집 요청 (신규 페이지)
- Google Analytics 미연결

#### 2026-08-18 에 끝낸 것 (다시 하지 말 것)
- **IndexNow 키** `c12d300d4481e5fcf77a58ae1b20a4d5.txt` 를 루트에 배치.
  발행기가 이 키를 찾아 발행할 때마다 새 글 주소를 즉시 통보한다(빙 계열).
  ⚠️ 이 파일을 지우면 통보가 조용히 멈춘다. 사이트맵에는 안 들어간다(정상).
  8/18 기존 5개 주소 수동 제출 완료 (HTTP 202).
- **하위 페이지 구조화 데이터** — blog=Blog / gallery=ImageGallery /
  media=CollectionPage + 각 BreadcrumbList. index.html·posts 는 원래 있었다.
- **글 템플릿 Article 에 image 추가** — 발행기가 템플릿 JSON-LD 를 복사해
  headline·날짜·URL·keywords 만 갈아끼우므로, 템플릿에 넣은 값은 앞으로
  올라가는 모든 글이 물려받는다. 반대로 말하면 **템플릿 JSON-LD 를 고칠 때는
  압축 한 줄 형식(공백 없는 separators)을 반드시 유지**해야 한다.
  형식이 깨지면 정규식이 안 맞아 새 글이 1번 글 제목을 그대로 달고 나간다.
- 소유확인은 메타태그 방식으로 구글·네이버·빙 **3사 모두 이미 등록됨**.

---

## 이번 세션(8/16~17)에 한 것

| | |
|---|---|
| 갤러리 페이지 | `gallery.html` 신설 — 16장, 분류 6종, 확대보기 |
| 방송·언론 페이지 | `media.html` 신설 — **14건**(영상 8/기사 6), 탭 없이 두 구역 상시 노출 |
| 자료 관리 화면 | `admin-media.html` 신설 — 실장님 입력용 + 제작사 직접 반영 |
| 모바일 메뉴 | 서랍·탭바·하위페이지 전부 수정 |
| 연락처 통일 | 0507 안심번호 → **043-645-3050** 29곳 |
| 상품 사진 | 4장 추가 → **9종 전부 완료** |
| 히어로 3번 | 20구 선물세트 사진으로 교체 |
| 가격 표기 | 매장 1팩 / 택배 1kg 병기 + 배송비 4,000원 |
| 스토어 링크 | 상품별 개별 연결 **6곳** |
| 상품명 | 쑥녹두 찹쌀떡 → **쑥 찹쌀떡** |
| 교재 | **V4(제5판)** 완성 — 로드맵·교시별 네비·영업논리 + 보완 15건 + 스크린샷 13장 |
| 스마트플레이스 | 상세설명 1,820자 · 찾아오는 길 332자 · 반려동물 안내 3안 |
| 견적서 | 정가 380만 → 청구 100만 구조 |

교재 체크리스트 19개 중 **17개** 완료. 남은 건 Google Analytics · 블로그 5편.

---

## 파일 구조 (신규분)

```
gallery.html        갤러리          → assets/js/gallery.js
media.html          방송·언론       → assets/js/media.js  + media.json
admin-media.html    자료 관리       → assets/js/admin-media.js + assets/css/admin.css
media.json          방송·언론 자료   ← 여기만 고치면 반영됨
assets/js/site-config.js   갤러리·상품주소·연락처 등 설정 한 곳
```

### 방송·언론 자료를 추가하려면
`media.json` 배열에 한 줄 추가하면 끝. 자료가 0건이면 메뉴·섹션이 통째로 숨겨진다.
```json
{ "type":"youtube", "youtubeId":"영상ID", "title":"제목", "outlet":"KBS", "date":"2024-05-13" }
{ "type":"article", "url":"기사주소",    "title":"제목", "outlet":"충북일보", "date":"2024-03-02" }
```
⚠️ **방송 화면 캡처·방송사 로고를 올리지 말 것.** 저작권 침해다. 제목 + 링크 방식으로만.

---

## 반드시 지킬 것 (함정 모음)

### CSS·JS·HTML 고치면 무조건
```bash
python bump-assets.py
```
모든 HTML × 모든 CSS/JS 를 훑어 해시를 붙인다. 빼먹으면 기능이 죽은 것처럼 보인다.

### 배포 직후 확인은 강력 새로고침
**HTML 자체가 10분 캐시된다**(GitHub Pages `max-age=600`). HTML 이 옛것이면 옛 `?v=` 를 읽어
결국 옛 CSS/JS 를 쓴다. `Ctrl+Shift+R` 또는 시크릿 창. 실제로 히어로 영상이 "사라졌다"고
오판해 진단에 시간을 썼다.

### PC 메뉴와 모바일 메뉴는 별개다
새 페이지를 메뉴에 넣으려면 **세 군데 모두** 고쳐야 한다.
- PC: `.nav-bar` (모바일에서는 `display:none`)
- 모바일: 햄버거 서랍 `.drawer` + 하단 탭바 `.tabbar` (5칸 한계)
- 하위 페이지(gallery/media/blog)는 서랍이 없어 `body.page-sub` 로 상단 메뉴를 모바일에도 표시

### AI 생성 사진의 글자는 믿지 말 것
호두정과 사진 라벨에 `043-545-3050` 이 찍혀 있었으나 **AI 가 지어낸 가짜**였다.
실제 번호는 `043-645-3050`. 라벨의 다른 글자도 전부 깨져 있었다.
→ 상품 사진 3장(쑥찹쌀떡·오란다·호두정과·동글이강정)은 AI 생성. 실물 사진으로 교체하면 더 낫다.

### 이미지 규격
- 히어로 배너 `1920×640` (3:1)
- 상품 사진 `800×800` (1:1)
- 용량: 히어로 ~300KB, 상품 ~200KB 이하

### llms.txt — 2026-08-18 해결됨 (예전 함정)
예전에는 `blog-auto/homepage_seo.build_llms_txt()` 가 발행할 때마다 llms.txt 를
통째로 새로 써서 업체 정보가 "글 목록"만 남기고 날아갔다.

이제 **`## 글 목록` 이 경계선**이다. 그 위(사람이 쓴 업체 정보)는 손대지 않고
아래만 발행기가 갱신한다. 저장소의 llms.txt 를 읽지 못하면 아예 건드리지 않는다.

- 발행기 수정 blog-auto `5e2f6e8` — **가비아 서버 반영 완료 2026-08-18**
  (서버는 git 이 아니라 scp 배포. 롤백본 `/home/ubuntu/blog-auto/_rollback/`)
- 이 저장소에 경계선 심음 `269690c`
- ⚠️ 업체 정보를 고칠 때는 **`## 글 목록` 윗부분만** 건드릴 것.
  아랫부분은 발행기가 관리하므로 손으로 고쳐도 다음 발행에 덮어써진다.

---

## 대표님이 직접 해야 하는 것 (우리가 통제 못 함)

| | 상태 |
|---|---|
| 네이버 플레이스 | 전화·영업시간 확인 필요. 로컬 검색의 기준점 |
| 스마트스토어 고객센터 번호 | 6개월마다 인증. 인증번호는 **사장님 폰(010-4658-3050)** 으로 감 |
| 스마트스토어 상품 등록 | 깨소금·두텁떡·오란다·호두정과·동글이강정 미등록 |
| 스마트스토어 정리 | "찹쌀떡-승검초단자 10구" 는 삭제 예정이라고 함 |
| 구글 비즈니스 | 전화는 이미 맞음. 영업시간만 확인 |

스마트플레이스에 넣을 문구는 `관리고객사\카페 떡\서류\` 에 준비되어 있다.
(상세설명·찾아오는길·반려동물안내 — 네이버 특수문자 규정 검사 통과)

---

## 남은 판단거리

- **승검초단자 30구** — 스토어에 없는데 홈페이지엔 있다. 답 없으면 일단 빼는 게 안전
- **모정소반** (유튜브 채널) — 명인떡과 어떤 관계인지 미확인. 현재 방송·언론 맨 뒤
- **네이버 블로그 2건** — 개인 후기라 언론 섹션에서 뺐다. 나중에 「방문 후기」 섹션 신설 시 활용
- **AI 생성 상품 사진** — 실물 사진으로 교체 권장
- **글해줌 미디어 관리** — 언론·갤러리를 글해줌에서 직접 올리게 하는 작업 (로드맵)

---

## 작업 기준

- 모든 변경은 **브라우저에서 실측 검증** (대비·겹침·overflow·콘솔)
- 확인되지 않은 사실은 쓰지 않는다 — 모르면 "확인 필요"로 두고 질문
- 커밋 메시지에 **원인 → 조치 → 검증** 기록
- 배포 후 반드시 실제 주소로 접속해 확인
